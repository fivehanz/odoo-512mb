# Managed by odoo-512mb repo (symlinked to /etc/sysctl.d/99-odoo.conf).

# With zram in place we WANT the kernel to spill to compressed RAM early.
vm.swappiness = 100

# Keep swap read-ahead small so zram does not waste IO on unneeded pages.
vm.page-cluster = 1